# generated from:
# $ pip install -r requirements.txt.in
# $ pip freeze > requirements.txt
# some dependencies are then manually commented out: these are provided in
# alpine py3-cryptography apk. if we try to install them from
# requirements.txt then alpine build will fail.
asn1crypto==0.24.0
certifi==2018.11.29
#cffi==1.11.5
chardet==3.0.4
#cryptography==2.5
decorator==4.3.2
#http-ece==1.1.0
idna==2.8
Mastodon.py==1.3.1
pycparser==2.19
python-dateutil==2.8.0
pytz==2018.9
requests==2.21.0
six==1.12.0
urllib3==1.24.1
